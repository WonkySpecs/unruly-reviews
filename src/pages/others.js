import React from "react"
import Layout from "../components/layout"

export default () => (
    <Layout>
        <div>This page lists the games that didn't make it (yet), and why.</div>
        <div>Split between "I just don't know them well enough" and "Not good enough". </div>
    </Layout>
)
